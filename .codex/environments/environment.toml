# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "mnimi"

[setup]
script = ""

[setup.linux]
script = '''
mise trust -y
mkdir -p "$CODEX_SOURCE_TREE_PATH/.superpowers"
cd "$CODEX_WORKTREE_PATH"
ln -s "$CODEX_SOURCE_TREE_PATH/apps/mobile/.env" apps/mobile/.env
ln -s "$CODEX_SOURCE_TREE_PATH/apps/server/.env" apps/server/.env
ln -s "$CODEX_SOURCE_TREE_PATH/.superpowers"
bun install --frozen-lockfile
bun db:migrate'''
